# Legacy Split Scripts

This folder keeps the earlier split-script workflow for reference only.

Use `../jupyter_copy_cell.py` as the active workflow. It is self-contained,
uses `../structures/` for source `.vasp` files, and is the version intended for
copying into Matlantis/Jupyter.

